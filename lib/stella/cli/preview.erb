
<%= " %-66s  ".att(:reverse) % ["#{@testplan.description}  (#{@testplan.gibbler.short})"] %><% @testplan.usecases.each_with_index do |uc,i| %>
 <%= (" %-61s %s%% ".att(:reverse).bright) % [uc.description, uc.ratio_pretty] %><% requests = uc.requests.each do |r| %>
  <%= " %-62s".bright % ["#{r.description}  (#{r.gibbler.short})"] %>
    <%= r %> <%= r.params.inspect %>
    <% if !r.headers.empty? %>Headers: 
      <% r.headers.each do |pair| %><%= pair.join(': ') %><% end %>
    <% end %><% if !r.response_handler.empty? %>Handler(s):
    <% r.response_handler.each do |status,proc| %><%= "   %s%s" % [status, proc.source.split($/).join("#{$/}   ")] %><% end %><% end %><% end %>
<% end %>
